const node_openssl = require('./index.js');
var fs = require('fs');
var openssl = new node_openssl();

/*fs.readFile('./test/encrypted.pem', function(err, contents) {
	//console.log(contents);
	openssl.importRSAPrivateKey(contents, '1234', function(err, key, cmd) {
		if(err) {
			console.log(err);
		} else {	
			console.log(key);
		}
	});
});*/

/*fs.readFile('./test/plain.pem', function(err, contents) {
        //console.log(contents);
        openssl.importRSAPrivateKey(contents, false, function(err, key, cmd) {
                if(err) {
                        console.log(err);
                } else {
                        console.log(key);
                }
        });
});*/

/*fs.readFile('./test/encrypted.der', function(err, contents) {
        //console.log(contents);
        openssl.importRSAPrivateKey(contents, false, function(err, key, cmd) {
                if(err) {
                        console.log(err);
                } else {
                        console.log(key);
                }
        });
});*/



fs.readFile('./test/encrypted.pfx', function(err, contents) {
        //console.log(contents);
        openssl.importRSAPrivateKey(contents, '', function(err, key, cmd) {
                if(err) {
                        console.log(err);
                } else {
                        console.log(key);
                }
        });
});
